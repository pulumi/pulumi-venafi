// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provisions a certificate from Venafi Control Plane's inventory to any of the supported Cloud Providers: Amazon
 * Certificate Manager, Azure KeyVault or Google Certificate Manager. Exports the ID of the provisioned certificate:
 * certificate name for AKV and GCM or ARN for ACM.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as venafi from "@pulumi/venafi";
 *
 * // Provision a certificate to cloud keystore with static values
 * const ckInstallationExampleById = new venafi.CloudKeystoreInstallation("ck_installation_example_by_id", {
 *     cloudKeystoreId: "e48897d0-2762-11ef-198k-79ac590dd358",
 *     certificateId: "1877af16-2762-11ef-8fab-cc123456ff7",
 *     cloudCertificateName: "com-terraform-example-com",
 * });
 * // Provision a certificate to cloud keystore
 * const ckInstallationExample = new venafi.CloudKeystoreInstallation("ck_installation_example", {
 *     cloudKeystoreId: ckExample.id,
 *     certificateId: certificateExample.certificateId,
 *     cloudCertificateName: certificateExample.commonName,
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import a Machine Identity from Venafi Control Plane using their ID. For example:
 *
 * console
 *
 * ```sh
 * $ pulumi import venafi:index/cloudKeystoreInstallation:CloudKeystoreInstallation example 2155bd32-2234-22ac-7cfd-ff1198845aa2
 * ```
 */
export class CloudKeystoreInstallation extends pulumi.CustomResource {
    /**
     * Get an existing CloudKeystoreInstallation resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CloudKeystoreInstallationState, opts?: pulumi.CustomResourceOptions): CloudKeystoreInstallation {
        return new CloudKeystoreInstallation(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'venafi:index/cloudKeystoreInstallation:CloudKeystoreInstallation';

    /**
     * Returns true if the given object is an instance of CloudKeystoreInstallation.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CloudKeystoreInstallation {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CloudKeystoreInstallation.__pulumiType;
    }

    /**
     * ARN of the AWS certificate. Use it to provision the VCP certificate to an existing ACM certificate, instead of a new one. Only valid for ACM keystores.
     */
    public readonly arn!: pulumi.Output<string | undefined>;
    /**
     * ID of the certificate to be provisioned to the given `keystoreId`.
     */
    public readonly certificateId!: pulumi.Output<string>;
    /**
     * The ID of the provisioned certificate within the Cloud Keystore
     */
    public /*out*/ readonly cloudCertificateId!: pulumi.Output<string>;
    /**
     * Metadata of the provisioned certificate from the Cloud Keystore
     */
    public /*out*/ readonly cloudCertificateMetadata!: pulumi.Output<{[key: string]: string}>;
    /**
     * Name for the provisioned certificate in the keystore. If the name already exists, the provisioning will replace the previous certificate with the one from `certificateId`. Only valid for AKV and GCM keystores.
     */
    public readonly cloudCertificateName!: pulumi.Output<string | undefined>;
    /**
     * ID of the cloud keystore where the certificate will be provisioned.
     */
    public readonly cloudKeystoreId!: pulumi.Output<string>;

    /**
     * Create a CloudKeystoreInstallation resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CloudKeystoreInstallationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CloudKeystoreInstallationArgs | CloudKeystoreInstallationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CloudKeystoreInstallationState | undefined;
            resourceInputs["arn"] = state ? state.arn : undefined;
            resourceInputs["certificateId"] = state ? state.certificateId : undefined;
            resourceInputs["cloudCertificateId"] = state ? state.cloudCertificateId : undefined;
            resourceInputs["cloudCertificateMetadata"] = state ? state.cloudCertificateMetadata : undefined;
            resourceInputs["cloudCertificateName"] = state ? state.cloudCertificateName : undefined;
            resourceInputs["cloudKeystoreId"] = state ? state.cloudKeystoreId : undefined;
        } else {
            const args = argsOrState as CloudKeystoreInstallationArgs | undefined;
            if ((!args || args.certificateId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'certificateId'");
            }
            if ((!args || args.cloudKeystoreId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'cloudKeystoreId'");
            }
            resourceInputs["arn"] = args ? args.arn : undefined;
            resourceInputs["certificateId"] = args ? args.certificateId : undefined;
            resourceInputs["cloudCertificateName"] = args ? args.cloudCertificateName : undefined;
            resourceInputs["cloudKeystoreId"] = args ? args.cloudKeystoreId : undefined;
            resourceInputs["cloudCertificateId"] = undefined /*out*/;
            resourceInputs["cloudCertificateMetadata"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CloudKeystoreInstallation.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CloudKeystoreInstallation resources.
 */
export interface CloudKeystoreInstallationState {
    /**
     * ARN of the AWS certificate. Use it to provision the VCP certificate to an existing ACM certificate, instead of a new one. Only valid for ACM keystores.
     */
    arn?: pulumi.Input<string>;
    /**
     * ID of the certificate to be provisioned to the given `keystoreId`.
     */
    certificateId?: pulumi.Input<string>;
    /**
     * The ID of the provisioned certificate within the Cloud Keystore
     */
    cloudCertificateId?: pulumi.Input<string>;
    /**
     * Metadata of the provisioned certificate from the Cloud Keystore
     */
    cloudCertificateMetadata?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * Name for the provisioned certificate in the keystore. If the name already exists, the provisioning will replace the previous certificate with the one from `certificateId`. Only valid for AKV and GCM keystores.
     */
    cloudCertificateName?: pulumi.Input<string>;
    /**
     * ID of the cloud keystore where the certificate will be provisioned.
     */
    cloudKeystoreId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CloudKeystoreInstallation resource.
 */
export interface CloudKeystoreInstallationArgs {
    /**
     * ARN of the AWS certificate. Use it to provision the VCP certificate to an existing ACM certificate, instead of a new one. Only valid for ACM keystores.
     */
    arn?: pulumi.Input<string>;
    /**
     * ID of the certificate to be provisioned to the given `keystoreId`.
     */
    certificateId: pulumi.Input<string>;
    /**
     * Name for the provisioned certificate in the keystore. If the name already exists, the provisioning will replace the previous certificate with the one from `certificateId`. Only valid for AKV and GCM keystores.
     */
    cloudCertificateName?: pulumi.Input<string>;
    /**
     * ID of the cloud keystore where the certificate will be provisioned.
     */
    cloudKeystoreId: pulumi.Input<string>;
}
